# lists/demo

One folder per imported list. Everything the scripts write here
(`dedupe_*.csv`, `dedupe_domains.txt`, `enrich.json`, `last_batch.txt`,
`inserted_*.csv`) is gitignored — only this README and `corrections.js`
are meant for git.

`corrections.js` shows the override format. Delete the example entries
before using this folder for a real list.
